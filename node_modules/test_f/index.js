var colors = require('colors');

colors.setTheme({
  silly: 'rainbow',
  input: 'grey',
  verbose: 'cyan',
  prompt: 'grey',
  info: 'green',
  data: 'grey',
  help: 'cyan',
  warn: 'yellow',
  debug: 'blue',
  error: 'red'
});

module.exports = {
	listen : function(globalVariable){
		globalVariable.event.on("user", function(data){
console.log(data.info);
});
globalVariable.event.on("accessories", function(data){
console.log(data.error);
});
	}
}
