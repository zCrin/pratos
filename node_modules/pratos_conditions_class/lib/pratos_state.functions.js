var cmd=require('node-cmd'); 
function  pratos_state(state, globalVariable){
	globalVariable.event.emit("system", state);
	cmd.get("sudo /etc/init.d/pratos "+state, function(err,data){
		console.log(err);
		console.log(data);
		process.exit(-1);
	});
}
module.exports= pratos_state;