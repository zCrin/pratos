var schedule = require('node-schedule'); functions = require('pratos_conditions_class').functions; module.exports ={ init : function(globalVariable){var Duration, Nbredeclap;


Nbredeclap = 0;
Duration = (Date.now());
globalVariable.event.on("Clapact",function(){  Nbredeclap = (typeof Nbredeclap == 'number' ? Nbredeclap : 0) + 1;
  if (Nbredeclap == 2 && (Date.now()) - Duration < 2000) {
    functions.homebridge_turnOn("Lampe bureau",globalVariable);
    Duration = (Date.now());
    Nbredeclap = 0;
  } else {
    Duration = (Date.now());
  }
});}};