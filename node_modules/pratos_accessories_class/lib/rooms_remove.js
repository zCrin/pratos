/*
* License MIT
* « Copyright © 2018, Pratos »
* v 1.3.0
*/
module.exports=remove;
function remove(user_id, globalVariable,callback){
	if(globalVariable[user_id].request.query.id){
		
		globalVariable.database.mongoDB.collection("rooms_list").deleteOne({_id: new globalVariable.database.ObjectID(globalVariable[user_id].request.query.id)},  function(err, obj) {
  
   
    
  });
		return callback('updated');
	}
	else{
		return callback('error');
	}
	
}